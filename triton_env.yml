# This is the conda environment used on triton.aalto.fi

name: medusa_flaq_qubit_compiler
channels:
  - conda-forge
dependencies:
  - numpy
  - matplotlib
  - python=3.10
  - cirq
  - julia
# the below need to be installed with pip:
#  - stim
#  - stimcirq
#  - qualtran